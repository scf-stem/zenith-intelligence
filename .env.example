# AI 学习助手 - 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ===== 必需配置 =====

# 智谱 ChatGLM API (统一密钥，用于文本和图像识别)
# 获取地址: https://open.bigmodel.cn/
CHATGLM_API_KEY=your_chatglm_api_key

# ===== 多模态模型配置 (图像识别) =====
# 默认使用 GLM-4.6V-FlashX
# 可选模型: glm-4.6v (旗舰版), glm-4.6v-flashx (轻量版), glm-4.6v-flash (免费版)
MULTIMODAL_MODEL=glm-4.6v-flashx
MULTIMODAL_API_URL=https://open.bigmodel.cn/api/paas/v4/chat/completions
# 留空时后端会自动回退到 CHATGLM_API_KEY
MULTIMODAL_API_KEY=

# 如果需要使用其他多模态服务，可配置以下选项:
# MULTIMODAL_API_KEY=your_multimodal_api_key
# MULTIMODAL_API_URL=https://api.example.com/v1/chat/completions

# ===== 可选配置 =====

# DeepSeek API (备选文本模型)
DEEPSEEK_API_KEY=your_deepseek_api_key

# JWT 密钥 (用于用户认证)
JWT_SECRET=your_jwt_secret_key

# 通义千问 (备选)
QWEN_API_KEY=your_qwen_api_key
QWEN_API_URL=https://dashscope.aliyuncs.com/api/v1/services/aigc/multimodal-generation/generation

# ===== 应用部署 =====

# 主应用端口
APP_PORT=8080
FLASK_ENV=production
PORT=8080

# 前端访问后端 API 的运行时地址。留空时同源访问；本地静态服务会自动回退到 http://localhost:5001。
VITE_API_BASE_URL=
